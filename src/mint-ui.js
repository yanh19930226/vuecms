import Vue from 'vue'
// 全局引入
import MintUI from 'mint-ui'
Vue.use(MintUI)
    // 按需引入
    // import { Header, TabItem, Button, Cell } from 'mint-ui'
    // Vue.component(Header.name, Header)
    // Vue.component(TabItem.name, TabItem)
    // Vue.component(Button.name, Button)
    // Vue.component(Cell.name, Cell)
    // Vue.use(Button)
    // Vue.use(Cell)